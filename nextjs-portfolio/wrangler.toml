name = "sharif-portfolio"
main = "worker/index.js"
compatibility_date = "2025-04-01"

# Serve the static files produced by `next build` (Next.js static export).
[assets]
directory = "./out"
binding = "ASSETS"