import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import Link from "next/link";
//import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <header>
          <h1>SSEBAYIGGA SHARIF</h1>
          <p>Front-End Developer</p>
          <Link href={""}>Home</Link>
          <Link href={""}>About</Link>
          <Link href={""}>Contact</Link>
          <Link href={""}>Why me</Link>
          <Link href={""}>Skills</Link>
        </header>
        {children}
        <footer>
          @copy 2025 SSEBAYIGGA SHARIF.
          <p>
            Let us connect :{" "}
            <Link href={"https://github.com/ssebayigga-sharif"}>Github</Link>
          </p>
          <p>
            {" "}
            <Link
              href={"https://www.linkedin.com/in/sharif-ssebayigga-7025a6375/"}
            >
              LinkedIn
            </Link>
          </p>
          <p>Email : sharifsseba@gmail.com.</p>
        </footer>
      </body>
    </html>
  );
}
